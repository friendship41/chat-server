spring:
  profiles:
    active: release
  application:
    name: chat_server
  cloud:
    config:
      uri: https://stage41.xyz:40000

management:
  endpoints:
    web:
      exposure:
        include: refresh
